node_modules/
.env
.env.local
**/build/
sim/out/
contracts/*.out.json
*.keystore
.next/

# Local tooling / secrets — NEVER commit (contains private keys)
.claude/
*.key
*.mnemonic

# Video recorder: keep the script, ignore deps + rendered output (goes to YouTube)
video/node_modules/
video/out/
video/package-lock.json
