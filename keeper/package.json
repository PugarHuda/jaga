{
  "name": "jaga-keeper",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Auto-roll keeper + settlement monitor for the Jaga vault on DeepBook Predict",
  "scripts": {
    "dev": "tsx watch --env-file=.env src/index.ts",
    "start": "tsx --env-file=.env src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.0.0",
    "typescript": "^5.4.0"
  }
}
